"use strict";
"use strict";
"use strict";

function toggleMenu() {
  var e = document.querySelector(".header__burger-menu"),
    t = document.querySelector(".header__burger-btn");
  e.classList.toggle("open"), t.classList.toggle("open");
}
$(function () {
  $('input[type="range"]').val(0), $('input[type="range"]').on("input", function () {
    var e = $(this).val();
    $(".input__range-value").text(e + "%");
  }), $(".input__range-value").text("0%");
}), $(function () {
  var e = {
    key: !0,
    dropBlock: $(".drop"),
    virtualSelect: $(".slct"),
    arrow: $(".arrow")
  };
  e.virtualSelect.on("click", function () {
    return e.dropBlock.is(":hidden") ? (e.dropBlock.slideDown(), $(this).addClass("active"), e.arrow.addClass("arrow--active")) : (e.dropBlock.slideUp(), $(this).delay(400).queue(function (e) {
      $(this).removeClass("active"), e();
    }), e.arrow.removeClass("arrow--active")), !1;
  }), e.dropBlock.find("li").on("click", function () {
    var t = $(this).html();
    return $(this).parent().parent().find("input").val(t), e.dropBlock.slideUp(), e.virtualSelect.delay(400).queue(function (e) {
      $(this).removeClass("active"), e();
    }).html(t), e.arrow.removeClass("arrow--active"), $(this).addClass("selected").siblings().removeClass("selected"), !1;
  }), $(document).on("click", function (t) {
    if (e.key && !$(t.target).closest(e.dropBlock).length) return e.dropBlock.slideUp(), e.virtualSelect.delay(400).queue(function (e) {
      $(this).removeClass("active"), e();
    }), e.arrow.removeClass("arrow--active"), void (e.key = !0);
  });
}), $(function () {
  var e = $(".placing-block__steps");
  for (var t = 0; t < 5; t++) e.prepend('\n      <div class="placing-block__points">\n        <div class="point"></div>\n        <div class="point"></div>\n        <div class="point"></div>\n        <div class="point"></div>\n        <div class="point"></div>\n      </div>\n    ');
}), $(function () {
  $('input[name="name"]').on("input", function () {
    var e = $(this).val();
    /^[а-яА-Я\s]+$/.test(e) ? $(this).removeClass("invalid").addClass("valid") : $(this).removeClass("valid").addClass("invalid");
  });
}), $(function () {
  $('input[name="e-mail"]').on("input", function () {
    var e = $(this).val();
    /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(e) ? $(this).removeClass("invalid").addClass("valid") : $(this).removeClass("valid").addClass("invalid");
  });
});